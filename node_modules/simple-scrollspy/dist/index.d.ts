import { ScrollSpy, type MenuElement, type Options } from './scrollspy';
declare const scrollSpy: (el: MenuElement, options?: Partial<Options>) => ScrollSpy;
export type { MenuElement, Options, ScrollSpy };
export default scrollSpy;
