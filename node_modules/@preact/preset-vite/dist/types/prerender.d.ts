import type { Plugin } from "vite";
interface PrerenderPluginOptions {
    prerenderScript?: string;
    renderTarget?: string;
    additionalPrerenderRoutes?: string[];
}
export declare function PrerenderPlugin({ prerenderScript, renderTarget, additionalPrerenderRoutes, }?: PrerenderPluginOptions): Plugin;
export {};
